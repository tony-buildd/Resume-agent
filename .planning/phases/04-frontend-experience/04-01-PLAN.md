---
phase: 04-frontend-experience
plan: 01
type: execute
wave: 1
depends_on:
  - "03-04"
files_modified:
  - apps/web/src/app/(workspace)/workspace/page.tsx
  - apps/web/src/app/globals.css
  - apps/web/src/components/workspace/workspace-shell.tsx
  - apps/web/src/components/workspace/artifact-panel.tsx
  - apps/web/src/lib/api/sessions.ts
autonomous: true
requirements:
  - UX-01
user_setup: []
must_haves:
  truths:
    - The conversation area becomes the visual primary surface.
    - The side panel defaults to the artifact most relevant to the active stage.
    - The shell still supports both resume and vault modes.
---

<objective>
Build the chat-centered shell and contextual panel system.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create the workspace shell and panel layout</name>
  <files>apps/web/src/components/workspace/workspace-shell.tsx, apps/web/src/app/(workspace)/workspace/page.tsx, apps/web/src/app/globals.css</files>
  <action>Replace the static diagnostic layout with a chat-centered shell plus contextual side panel.</action>
</task>

<task type="auto">
  <name>Task 2: Add kind-aware artifact panel rendering</name>
  <files>apps/web/src/components/workspace/artifact-panel.tsx, apps/web/src/lib/api/sessions.ts</files>
  <action>Introduce typed rendering for the active artifact kinds and stage-aware default panel selection.</action>
</task>

</tasks>

<verification>
- [ ] Workspace primary column reads like a guided conversation, not a raw dashboard
- [ ] Side panel changes context based on the active stage
- [ ] Existing resume and vault modes still render
</verification>

<output>
After completion, create `.planning/phases/04-frontend-experience/04-01-SUMMARY.md`
</output>
