---
phase: 03-resume-session-flow
plan: 01
type: execute
wave: 1
depends_on:
  - "01-03"
  - "02-03"
files_modified:
  - apps/api/pyproject.toml
  - apps/api/app/config.py
  - apps/api/app/llm/openai_client.py
  - apps/api/app/orchestration/contracts.py
  - apps/api/app/orchestration/runtime.py
  - apps/api/app/orchestration/research.py
autonomous: true
requirements:
  - RSCH-01
  - RSCH-02
  - RSCH-03
user_setup:
  - OPENAI_API_KEY
must_haves:
  truths:
    - JD analysis produces a structured artifact with requirement, archetype, and success signals.
    - Company/role research is cited and stored separately from the strategic summary.
    - The flow attaches to the existing session runtime and pauses for JD analysis approval.
  artifacts:
    - apps/api/app/orchestration/research.py
    - apps/api/app/llm/openai_client.py
  key_links:
    - Responses API integration stays behind a provider adapter.
    - Research outputs remain inspectable through artifacts and traces.
---

<objective>
Implement JD analysis and cited research on the session runtime.

Purpose: Turn the raw job description into a structured and inspectable strategy input.
Output: OpenAI-backed analysis/research adapter, runtime stages for JD analysis + research summary, and an approval gate before downstream strategy work.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add provider configuration and OpenAI adapter</name>
  <files>apps/api/pyproject.toml, apps/api/app/config.py, apps/api/app/llm/openai_client.py</files>
  <action>Add the OpenAI dependency and a small Responses-API adapter for structured outputs and tool-using calls. Keep provider details isolated from runtime logic.</action>
  <acceptance_criteria>
    - OpenAI config is explicit and local-dev friendly
    - Runtime code can call a typed adapter rather than raw provider SDK calls
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add JD analysis and research stage services</name>
  <files>apps/api/app/orchestration/research.py, apps/api/app/orchestration/contracts.py</files>
  <action>Create typed JD analysis, cited research, and strategic summary contracts plus the helper functions that produce them.</action>
  <acceptance_criteria>
    - Structured JD analysis contract exists
    - Research results include citations or source links
    - Strategic summary is stored separately from raw research findings
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Attach JD analysis approval to the runtime</name>
  <files>apps/api/app/orchestration/runtime.py</files>
  <action>Extend the main session flow so it captures the JD, runs analysis/research, stores artifacts, and interrupts for approval before moving to interrogation.</action>
  <acceptance_criteria>
    - Session pauses for missing JD input
    - Session pauses again for JD analysis approval
    - Artifacts and traces reflect the research chain
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] JD analysis artifact exists with structured requirement output
- [ ] Research artifact includes citations
- [ ] Session cannot advance past strategy lock without approval
</verification>

<output>
After completion, create `.planning/phases/03-resume-session-flow/03-01-SUMMARY.md`
</output>
