---
phase: 01-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/web/src/middleware.ts
  - apps/web/src/app/(workspace)/layout.tsx
  - apps/web/src/lib/auth.ts
  - apps/api/app/config.py
  - apps/api/app/db/session.py
  - apps/api/app/db/models.py
  - apps/api/app/api/routes/health.py
  - apps/api/app/api/routes/sessions.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - The web app has protected workspace routing based on authenticated user context.
    - The API can persist user-scoped session records in Postgres-backed models.
    - The project has a single source of truth for app-level user/session ownership.
  artifacts:
    - apps/web/src/middleware.ts
    - apps/web/src/lib/auth.ts
    - apps/api/app/db/models.py
    - apps/api/app/api/routes/sessions.py
  key_links:
    - Web auth helpers and route protection align with Clerk-backed session expectations.
    - API session models and routes enforce per-user ownership boundaries.
---

<objective>
Wire the authentication and persistence foundation for Resume Agent.

Purpose: Establish secure user scoping and the durable session storage that later vault and orchestration stages depend on.
Output: Clerk-aware frontend auth shell plus backend session models/routes backed by Postgres.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/1-CONTEXT.md
@.planning/phases/01-foundations/1-RESEARCH.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add frontend auth boundaries for the workspace shell</name>
  <files>apps/web/src/middleware.ts, apps/web/src/app/(workspace)/layout.tsx, apps/web/src/lib/auth.ts</files>
  <read_first>apps/web/src/app/layout.tsx, .planning/phases/01-foundations/1-CONTEXT.md</read_first>
  <action>Integrate Clerk at the web app boundary so protected workspace routes require an authenticated user. Create a small auth helper layer rather than scattering auth checks across components. Keep route structure ready for a workspace shell even if the page content remains basic in this phase.</action>
  <verify>test -f apps/web/src/middleware.ts && rg -n \"Clerk|auth|workspace\" apps/web/src</verify>
  <acceptance_criteria>
    - Protected workspace routing exists
    - Auth access logic is centralized in a small helper/module
  </acceptance_criteria>
  <done>The web app has a real authenticated boundary rather than a public placeholder shell.</done>
</task>

<task type="auto">
  <name>Task 2: Add backend config and Postgres session persistence</name>
  <files>apps/api/app/config.py, apps/api/app/db/session.py, apps/api/app/db/models.py</files>
  <read_first>apps/api/app/main.py, .planning/REQUIREMENTS.md</read_first>
  <action>Create backend configuration and database session setup for Postgres, then define initial models for user-linked sessions and artifacts/events as phase-one persistence primitives. Do not model the full career vault yet; keep the schema focused on user, session, artifact, and trace/event ownership.</action>
  <verify>test -f apps/api/app/db/models.py && test -f apps/api/app/db/session.py</verify>
  <acceptance_criteria>
    - Database config/session modules exist
    - Models include at least user-scoped session and trace/event persistence primitives
  </acceptance_criteria>
  <done>Backend persistence exists for user-owned session lifecycle state.</done>
</task>

<task type="auto">
  <name>Task 3: Expose minimal API routes for health and session lifecycle</name>
  <files>apps/api/app/api/routes/health.py, apps/api/app/api/routes/sessions.py, apps/api/app/main.py</files>
  <read_first>apps/api/app/db/models.py, apps/api/app/config.py</read_first>
  <action>Add a health route and the minimal session lifecycle endpoints needed to create/read user-scoped session envelopes. Keep contracts narrow and typed so later orchestration work extends these endpoints instead of replacing them.</action>
  <verify>rg -n \"APIRouter|session|health\" apps/api/app/api/routes apps/api/app/main.py</verify>
  <acceptance_criteria>
    - API has a health route
    - API has typed session endpoints connected to persistence
  </acceptance_criteria>
  <done>There is a concrete backend contract for session state and ownership.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Backend tests or direct checks prove the session model can persist and read a user-owned session
- [ ] Workspace protection blocks unauthenticated access
- [ ] Session endpoints return typed responses, not ad hoc blobs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The system now has a real authenticated boundary and user-scoped persistence foundation
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-02-SUMMARY.md`
</output>
