---
phase: 01-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - apps/web/package.json
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/page.tsx
  - apps/api/pyproject.toml
  - apps/api/app/main.py
  - README.md
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - The repo has a runnable split between the web app and API service.
    - Developers have one documented way to install dependencies and boot both apps locally.
  artifacts:
    - package.json
    - pnpm-workspace.yaml
    - apps/web/package.json
    - apps/web/src/app/layout.tsx
    - apps/api/pyproject.toml
    - apps/api/app/main.py
  key_links:
    - Root workspace tooling references both apps/web and apps/api.
    - README local setup instructions match the commands exposed by the repo.
---

<objective>
Create the initial repo and runtime skeleton for Resume Agent.

Purpose: Establish the app boundaries and local tooling foundation that every later phase depends on.
Output: A runnable workspace skeleton with `apps/web`, `apps/api`, root tooling, and bootstrap documentation.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/1-CONTEXT.md
@.planning/phases/01-foundations/1-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the root workspace and app directories</name>
  <files>package.json, pnpm-workspace.yaml, apps/web/package.json, apps/api/pyproject.toml, README.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/phases/01-foundations/1-CONTEXT.md</read_first>
  <action>Create the root workspace/tooling files and the base `apps/web` and `apps/api` directories. Use `pnpm` for the JS workspace and `uv`-friendly Python metadata for the API. Keep the structure simple and explicit: no unnecessary packages beyond what is needed to boot a minimal web shell and API service.</action>
  <verify>test -f package.json && test -f pnpm-workspace.yaml && test -f apps/api/pyproject.toml</verify>
  <acceptance_criteria>
    - `pnpm-workspace.yaml` contains `apps/*`
    - `package.json` contains workspace-level scripts for web and api development
    - `apps/api/pyproject.toml` exists and defines the backend project metadata
  </acceptance_criteria>
  <done>Root tooling and app directories exist with consistent bootstrap scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Create minimal web and API entrypoints</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/page.tsx, apps/api/app/main.py</files>
  <read_first>apps/web/package.json, apps/api/pyproject.toml</read_first>
  <action>Add a minimal authenticated-workspace placeholder page in the web app and a minimal FastAPI application entrypoint in the API app. Keep the web page intentionally sparse but brand it as Resume Agent so later auth/session work has a clear landing shell.</action>
  <verify>test -f apps/web/src/app/page.tsx && test -f apps/api/app/main.py</verify>
  <acceptance_criteria>
    - `apps/web/src/app/page.tsx` renders a Resume Agent workspace placeholder
    - `apps/api/app/main.py` exposes a FastAPI application object
  </acceptance_criteria>
  <done>Both apps have concrete entrypoints that later plans can extend instead of replacing.</done>
</task>

<task type="auto">
  <name>Task 3: Document the local bootstrap path</name>
  <files>README.md</files>
  <read_first>package.json, apps/api/pyproject.toml</read_first>
  <action>Update `README.md` so it explains the project purpose briefly and documents the intended local setup commands for the workspace and API skeleton. Keep it accurate to the files and scripts created in this plan; do not promise later-phase features.</action>
  <verify>rg -n \"Resume Agent|apps/web|apps/api|pnpm|uv\" README.md</verify>
  <acceptance_criteria>
    - `README.md` describes the split app structure
    - `README.md` includes concrete local setup commands
  </acceptance_criteria>
  <done>The repo can be understood and bootstrapped without opening planning docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install` succeeds at the root
- [ ] A backend env sync/install command derived from `apps/api/pyproject.toml` succeeds
- [ ] `README.md` matches the actual bootstrap commands and file layout
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo now has a concrete frontend/backend split ready for auth and persistence work
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-01-SUMMARY.md`
</output>
