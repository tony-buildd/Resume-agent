---
phase: 01-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - apps/api/app/orchestration/contracts.py
  - apps/api/app/orchestration/runtime.py
  - apps/api/app/api/routes/sessions.py
  - apps/web/src/lib/api/sessions.ts
  - apps/web/src/app/(workspace)/page.tsx
  - docs/architecture/foundation-contracts.md
autonomous: true
requirements:
  - FLOW-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - A session can move through a typed runtime shell until it reaches an interrupt or completion boundary.
    - Session artifacts and trace events are persisted in a way later phases can render and review.
    - The workspace can read and display basic session state from the API.
  artifacts:
    - apps/api/app/orchestration/contracts.py
    - apps/api/app/orchestration/runtime.py
    - apps/web/src/lib/api/sessions.ts
    - apps/web/src/app/(workspace)/page.tsx
    - docs/architecture/foundation-contracts.md
  key_links:
    - API session routes invoke the orchestration shell through typed contracts.
    - The workspace session page reads persisted session state rather than hardcoded placeholders.
---

<objective>
Create the orchestration shell and observable session contract for Resume Agent.

Purpose: Give later phases a stable runtime for artifacts, stage transitions, approvals, and traceability.
Output: Typed orchestration contracts, runtime shell, session API wiring, and a workspace page that can render persisted session state.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundations/1-CONTEXT.md
@.planning/phases/01-foundations/1-RESEARCH.md
@.planning/phases/01-foundations/01-01-SUMMARY.md
@.planning/phases/01-foundations/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define orchestration contracts and runtime shell</name>
  <files>apps/api/app/orchestration/contracts.py, apps/api/app/orchestration/runtime.py</files>
  <read_first>.planning/phases/01-foundations/1-CONTEXT.md, apps/api/app/api/routes/sessions.py</read_first>
  <action>Create the first typed orchestration models and runtime shell for a session. The shell should support the phase-one rule that sessions auto-run until the next interrupt, approval gate, or completion, even if the internal logic is still skeletal. Keep stage state and artifact state explicit.</action>
  <verify>test -f apps/api/app/orchestration/contracts.py && test -f apps/api/app/orchestration/runtime.py</verify>
  <acceptance_criteria>
    - Contract models exist for stage/session/artifact state
    - Runtime shell exposes an explicit advance/resume boundary
  </acceptance_criteria>
  <done>The backend has a typed runtime shell instead of ad hoc session logic.</done>
</task>

<task type="auto">
  <name>Task 2: Connect session routes and frontend API access to persisted session state</name>
  <files>apps/api/app/api/routes/sessions.py, apps/web/src/lib/api/sessions.ts, apps/web/src/app/(workspace)/page.tsx</files>
  <read_first>apps/api/app/orchestration/contracts.py, apps/api/app/db/models.py</read_first>
  <action>Update session routes so they can create/read the orchestration session envelope and trace/event data. Add a small frontend API client and a workspace page that fetches and renders the basic session state, including stage summary and artifact/event placeholders.</action>
  <verify>rg -n \"session|artifact|trace|stage\" apps/api/app/api/routes/sessions.py apps/web/src/lib/api/sessions.ts apps/web/src/app/(workspace)/page.tsx</verify>
  <acceptance_criteria>
    - Session API returns persisted stage/session state
    - Workspace page reads live session data via a dedicated client module
  </acceptance_criteria>
  <done>The frontend is reading the real session model, not a mock page.</done>
</task>

<task type="auto">
  <name>Task 3: Document the foundation contracts for future phases</name>
  <files>docs/architecture/foundation-contracts.md</files>
  <read_first>apps/api/app/orchestration/contracts.py, .planning/ROADMAP.md</read_first>
  <action>Write a concise architecture note that explains the phase-one contract surfaces: auth boundary, session envelope, artifact persistence, trace/event model, and how future phases should extend them. Keep it implementation-specific enough to prevent later drift.</action>
  <verify>test -f docs/architecture/foundation-contracts.md && rg -n \"artifact|trace|session|stage\" docs/architecture/foundation-contracts.md</verify>
  <acceptance_criteria>
    - The document explains the session/artifact/trace contracts
    - Future phases have a concrete reference for extending the foundation
  </acceptance_criteria>
  <done>Foundation contracts are documented and no longer implicit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The API can create and return a typed session envelope with stage and artifact/event fields
- [ ] The workspace page renders fetched session state successfully
- [ ] Contract documentation matches the implemented runtime and persistence model
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The foundation now supports later vault, research, and drafting phases without redefining core session contracts
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations/01-03-SUMMARY.md`
</output>
