---
phase: 02-career-vault
plan: 01
type: execute
wave: 1
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - apps/api/app/db/models.py
  - apps/api/app/vault/contracts.py
  - apps/api/app/vault/service.py
  - apps/api/app/api/routes/vault.py
  - apps/api/app/main.py
autonomous: true
requirements:
  - VAULT-03
  - VAULT-04
  - VAULT-05
user_setup: []
must_haves:
  truths:
    - The backend has canonical entities for company, role, project story, fact, and bullet candidate storage.
    - Facts and bullets carry explicit provenance/review state rather than implicit conventions.
    - The API exposes typed user-scoped vault primitives that later ingestion and retrieval flows can build on.
  artifacts:
    - apps/api/app/db/models.py
    - apps/api/app/vault/contracts.py
    - apps/api/app/vault/service.py
    - apps/api/app/api/routes/vault.py
  key_links:
    - Vault entities extend the Phase 1 user/session persistence model without bypassing ownership boundaries.
    - Typed vault contracts mirror the explicit artifact/session contract style established in Phase 1.
---

<objective>
Design and implement the canonical Career Vault schema and API primitives.

Purpose: Establish the durable relational memory layer for companies, roles, project stories, facts, provenance, and bullet candidates.
Output: Postgres-backed vault entities plus typed service and route boundaries for user-scoped vault data.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-career-vault/2-CONTEXT.md
@.planning/phases/02-career-vault/2-RESEARCH.md
@.planning/phases/01-foundations/01-02-SUMMARY.md
@.planning/phases/01-foundations/01-03-SUMMARY.md
@docs/architecture/foundation-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the database model with canonical vault entities</name>
  <files>apps/api/app/db/models.py</files>
  <read_first>.planning/phases/02-career-vault/2-CONTEXT.md, apps/api/app/db/models.py</read_first>
  <action>Add normalized models for vault companies, roles, project stories, facts, and bullet candidates. Keep all records user-scoped through the existing app user ownership model. Facts and bullets must include explicit provenance/review state fields that later phases can filter on safely.</action>
  <verify>rg -n "Vault|Fact|Bullet|ProjectStory|Role" apps/api/app/db/models.py</verify>
  <acceptance_criteria>
    - Canonical vault entities exist in the ORM layer
    - Facts and bullets include provenance/review state fields
    - Ownership boundaries remain anchored to `AppUser`
  </acceptance_criteria>
  <done>The database can store structured vault memory without flattening it into resume text.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed vault contracts and service-layer primitives</name>
  <files>apps/api/app/vault/contracts.py, apps/api/app/vault/service.py</files>
  <read_first>apps/api/app/orchestration/contracts.py, .planning/phases/02-career-vault/2-RESEARCH.md</read_first>
  <action>Create typed request/response contracts and a service layer for basic vault creation and listing behavior. Keep the service focused on canonical entity persistence and serialization, not ingestion or retrieval heuristics yet.</action>
  <verify>test -f apps/api/app/vault/contracts.py && test -f apps/api/app/vault/service.py</verify>
  <acceptance_criteria>
    - Vault contracts follow the typed explicit style of the session envelope
    - Service helpers encapsulate ORM write/read behavior for vault entities
  </acceptance_criteria>
  <done>The vault has a real backend contract surface instead of direct route-to-ORM coupling.</done>
</task>

<task type="auto">
  <name>Task 3: Expose user-scoped vault routes</name>
  <files>apps/api/app/api/routes/vault.py, apps/api/app/main.py</files>
  <read_first>apps/api/app/api/routes/sessions.py, apps/api/app/vault/service.py</read_first>
  <action>Add minimal typed routes for creating and listing vault entities under the authenticated user boundary. Keep the API narrow and stable so later ingestion and retrieval plans can extend it instead of replacing it.</action>
  <verify>rg -n "vault" apps/api/app/api/routes apps/api/app/main.py</verify>
  <acceptance_criteria>
    - Vault routes are mounted on the API
    - Vault routes are user-scoped and typed
  </acceptance_criteria>
  <done>The backend exposes a concrete user-owned Career Vault foundation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The API can create and return typed vault entities for a single user
- [ ] Facts and bullets round-trip with explicit provenance/review state
- [ ] User-scoped access boundaries are preserved for vault routes
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The project now has a canonical Career Vault schema ready for ingestion and retrieval work
</success_criteria>

<output>
After completion, create `.planning/phases/02-career-vault/02-01-SUMMARY.md`
</output>
