---
phase: 02-career-vault
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/app/vault/ingestion.py
  - apps/api/app/api/routes/vault.py
  - apps/api/app/orchestration/contracts.py
  - apps/api/app/orchestration/runtime.py
  - apps/web/src/lib/api/vault.ts
  - apps/web/src/app/(workspace)/workspace/page.tsx
autonomous: true
requirements:
  - VAULT-01
  - VAULT-02
  - VAULT-04
user_setup: []
must_haves:
  truths:
    - Users can seed the vault from imported materials and continue through a role-by-role interview flow.
    - Ingestion stays within one role or project story thread at a time instead of blending the whole career at once.
    - Story checkpoint review exists so users can validate a coherent captured role before moving on.
  artifacts:
    - apps/api/app/vault/ingestion.py
    - apps/web/src/lib/api/vault.ts
    - apps/web/src/app/(workspace)/workspace/page.tsx
  key_links:
    - Vault ingestion attaches to the existing session/orchestration shell rather than bypassing it.
    - Review checkpoints preserve the provenance/review model from the canonical vault schema.
---

<objective>
Build the import and interview-first Career Vault ingestion flows.

Purpose: Let users seed the vault from existing materials and deepen it through focused role/project questioning with checkpoint review behavior.
Output: Vault ingestion services, routes, and a minimal workspace interaction surface for import plus role-story capture.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-career-vault/2-CONTEXT.md
@.planning/phases/02-career-vault/2-RESEARCH.md
@.planning/phases/02-career-vault/02-01-SUMMARY.md
@docs/architecture/foundation-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add seed import and guided ingestion services</name>
  <files>apps/api/app/vault/ingestion.py, apps/api/app/api/routes/vault.py</files>
  <read_first>apps/api/app/vault/service.py, .planning/phases/02-career-vault/2-CONTEXT.md</read_first>
  <action>Create backend ingestion primitives for seed materials and guided role/project capture. Keep the seed path simple but structured, and ensure the guided path can capture one coherent role/project thread without flattening multiple stories together.</action>
  <verify>rg -n "ingest|seed|checkpoint|story" apps/api/app/vault apps/api/app/api/routes/vault.py</verify>
  <acceptance_criteria>
    - Seed import entrypoint exists
    - Guided role/project capture entrypoint exists
    - Ingestion output maps into canonical vault entities
  </acceptance_criteria>
  <done>The vault can be populated from both imported materials and focused interviews.</done>
</task>

<task type="auto">
  <name>Task 2: Attach vault ingestion to the session runtime</name>
  <files>apps/api/app/orchestration/contracts.py, apps/api/app/orchestration/runtime.py</files>
  <read_first>apps/api/app/orchestration/runtime.py, apps/api/app/vault/ingestion.py</read_first>
  <action>Extend the runtime shell so vault ingestion can use resumable session boundaries, question prompts, and review checkpoints instead of living as a disconnected background process. Keep the contract explicit so later phases can inspect vault-ingestion traces and artifacts.</action>
  <verify>rg -n "vault|checkpoint|interview|review" apps/api/app/orchestration/contracts.py apps/api/app/orchestration/runtime.py</verify>
  <acceptance_criteria>
    - Vault ingestion stages can interrupt and resume through the existing runtime shell
    - Review checkpoint state is explicit in runtime artifacts or stage data
  </acceptance_criteria>
  <done>The vault fits the existing agent/session model instead of becoming a side-channel.</done>
</task>

<task type="auto">
  <name>Task 3: Add minimal workspace support for vault ingestion</name>
  <files>apps/web/src/lib/api/vault.ts, apps/web/src/app/(workspace)/workspace/page.tsx</files>
  <read_first>apps/web/src/lib/api/sessions.ts, apps/web/src/app/(workspace)/workspace/page.tsx</read_first>
  <action>Add a small authenticated client for vault endpoints and extend the workspace page enough to display seed-import/interview checkpoint information. Keep the UI narrow: this phase only needs to prove the flow exists, not deliver the final workspace UX.</action>
  <verify>rg -n "vault|import|story|checkpoint" apps/web/src/lib/api apps/web/src/app/(workspace)/workspace/page.tsx</verify>
  <acceptance_criteria>
    - Web app can call vault ingestion endpoints through a dedicated client
    - Workspace can surface at least one import/interview checkpoint state
  </acceptance_criteria>
  <done>The Career Vault ingestion flow is visible and testable from the workspace shell.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] A user can create vault data through a seed or guided-ingestion path
- [ ] Guided capture remains scoped to one role/project thread at a time
- [ ] A checkpoint review state is visible in persisted runtime or artifact data
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Career Vault can now be populated and reviewed incrementally
</success_criteria>

<output>
After completion, create `.planning/phases/02-career-vault/02-02-SUMMARY.md`
</output>
