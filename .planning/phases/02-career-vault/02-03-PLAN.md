---
phase: 02-career-vault
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - apps/api/pyproject.toml
  - apps/api/app/config.py
  - apps/api/app/vault/retrieval.py
  - apps/api/app/vault/indexing.py
  - apps/api/app/api/routes/vault.py
  - apps/web/src/lib/api/vault.ts
autonomous: true
requirements:
  - VAULT-05
  - VAULT-06
user_setup: []
must_haves:
  truths:
    - Retrieval can distinguish draft-safe approved material from questioning-only inferred material.
    - Long-form notes and prior answers are indexed semantically without replacing canonical Postgres storage.
    - The API can return multiple candidate bullets for one experience while respecting review/provenance state.
  artifacts:
    - apps/api/app/vault/retrieval.py
    - apps/api/app/vault/indexing.py
    - apps/api/app/api/routes/vault.py
  key_links:
    - Retrieval merges canonical relational filters with Chroma-backed semantic recall.
    - Questioning-safe and drafting-safe outputs are explicitly separated in the returned contract.
---

<objective>
Add retrieval rules and semantic indexing for the Career Vault.

Purpose: Make the vault actually reusable by later JD/session stages without violating the trust model around unapproved inferred facts.
Output: Retrieval and indexing services plus typed APIs for draft-safe and questioning-safe vault recall.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-career-vault/2-CONTEXT.md
@.planning/phases/02-career-vault/2-RESEARCH.md
@.planning/phases/02-career-vault/02-01-SUMMARY.md
@.planning/phases/02-career-vault/02-02-SUMMARY.md
@docs/architecture/foundation-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Chroma-backed indexing for long-form vault notes</name>
  <files>apps/api/pyproject.toml, apps/api/app/config.py, apps/api/app/vault/indexing.py</files>
  <read_first>.planning/phases/02-career-vault/2-RESEARCH.md, apps/api/app/config.py</read_first>
  <action>Add the dependencies and configuration needed for Chroma-backed indexing of imported notes and prior answers. Keep the adapter isolated so canonical entity writes still go to Postgres first and indexing can be retried or rebuilt separately.</action>
  <verify>rg -n "chroma|embedding|collection" apps/api/pyproject.toml apps/api/app/config.py apps/api/app/vault/indexing.py</verify>
  <acceptance_criteria>
    - Chroma configuration exists behind explicit backend settings
    - Indexing logic is isolated from canonical Postgres writes
  </acceptance_criteria>
  <done>Long-form vault material can be semantically indexed without changing the source-of-truth model.</done>
</task>

<task type="auto">
  <name>Task 2: Add retrieval services with review-state filtering</name>
  <files>apps/api/app/vault/retrieval.py, apps/api/app/api/routes/vault.py</files>
  <read_first>apps/api/app/vault/service.py, apps/api/app/vault/indexing.py</read_first>
  <action>Create retrieval helpers that return draft-safe approved material separately from questioning-safe inferred material. The service must be able to return many candidate bullets for one experience while keeping review-state boundaries explicit.</action>
  <verify>rg -n "draft|question|approved|inferred|retrieve" apps/api/app/vault/retrieval.py apps/api/app/api/routes/vault.py</verify>
  <acceptance_criteria>
    - Retrieval results separate drafting-safe and questioning-safe outputs
    - Multiple candidate bullets can be returned for one role/story
  </acceptance_criteria>
  <done>The vault can now support later questioning and drafting phases without trust-model leakage.</done>
</task>

<task type="auto">
  <name>Task 3: Expose typed retrieval access to the web app</name>
  <files>apps/web/src/lib/api/vault.ts</files>
  <read_first>apps/api/app/api/routes/vault.py, apps/web/src/lib/api/sessions.ts</read_first>
  <action>Extend the web API client so later session and workspace phases can request vault recall through a dedicated typed surface. Keep the client contracts narrow and aligned to the backend retrieval distinction between questioning-safe and draft-safe results.</action>
  <verify>rg -n "draftSafe|questioningSafe|vault" apps/web/src/lib/api/vault.ts</verify>
  <acceptance_criteria>
    - Web app has a dedicated vault retrieval client
    - Client types reflect the draft-safe vs questioning-safe separation
  </acceptance_criteria>
  <done>The frontend has a stable client surface for later session-stage retrieval integration.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Chroma indexing is configured without becoming the canonical store
- [ ] Retrieval excludes unapproved inferred facts from draft-safe outputs
- [ ] Retrieval can still surface inferred material for questioning-safe outputs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The Career Vault now supports safe reuse in later JD/session stages
</success_criteria>

<output>
After completion, create `.planning/phases/02-career-vault/02-03-SUMMARY.md`
</output>
